# 星网产品 — 实时进度

> 📦 **星网文档集 v3** · 对齐于 2026/06/08
>
> **目的:** 记录当前进度位置、已拍的关键决策、产品状态。每次接续工作,从这份开始读最快进入状态。
> **这份只管"现在到哪了";"怎么做"看执行计划,"为什么做"看产品 plan。**

---

## ⭐ 最新对话结论(最重要,先读这块)

**🔴🔴 重大重立:星网从"跨工具记忆"掉头到"记忆治理层"(2026-06-09 晚,创始人拍板 · 最高优先级,先读这块)**
- **触发:** 创始人当第一个用户自检,直说 **"换多个工具切换实际上不痛,我就用一个工具,跨工具只是捎带手"** → 否掉了支撑旧定位的"多工具记忆"核心假设。**整个"跨厂归属/通用记忆层/本地版Mem.ai"定位作废,不能打补丁(地基塌了)。**
- **真痛重定位(创始人确认 A=真疼):** 不是"换工具记忆没了",是 **"AI 记忆/工作记录越用越多、越乱、越膨胀,AI 拿着乱东西帮倒忙,没人帮你治理"**——**单工具就发生**;创始人 2026-06-06 搭 Claude 环境亲手撞过、现在还被烦(见 `地基层_需求来源_创始人手搭复盘.md` / memory `starnet-thesis-lived-validation`)。
- **🏛️ 产品重立定义(创始人认,含"归用户"):** **星网 = 你的 AI 记忆管家——你攒的东西越来越多越乱,星网帮你 ① 看见 ② 自动减负,让 AI 永远拿一份"干净、且归你自己"的"你"干活,而不是一坨越滚越乱、还不归你的垃圾。** 三柱:**看见(镜子)+ 减负(新陈代谢/半自动整理)+ 归你(本地主权)**;**跨工具降为捎带手**。
- **为何比旧定位强(非安慰):** ① **不在红海**——Mem.ai 等全在"记更多+喂进去"(囤积工具),星网是**反面**:看见+减下去+清爽。② **大厂结构上不做**——它们巴不得你记忆越攒越多(越粘越锁定),"帮你减/治理"对它们=自残。③ **越往后越值钱**——大厂记忆越加越多,人的记忆只会越乱,治理需求随时间变大。
- **不是从零掉头:** 原"灵魂层"(节点图镜子 + 新陈代谢熄灭/减负 + 半自动蒸馏整理)一直就是这个真痛的解药——**把它从"脸"扶正为"核心头牌"**;把跨工具从头牌降为捎带手。错的只是头上那块招牌,灵魂一直对。
- **🔻 返工债(下一步,谈工程架构前先清):** 现有 `星网_产品全局plan.md`/`星网_执行计划.md`/`参考文档/星网_记忆引擎设计.md` 等全围着旧"跨工具核心"写,已与重立不符,**需逐份返工对齐到"记忆治理层"**。详见 memory `starnet-competitive-landscape-2026-06`(已记 2026-06 竞品重查 + 本次重立)。
- **创始人定的顺序:** 先返工文档(选了路1:先返工再谈架构) → 再谈点名的**核心命门「记忆工程架构」**(下面那块 🏛️,仍有效,现在要服务于重立后的"治理层"产品)。
- **🛑 本轮精确停靠点(下次从这接):**
  1. ✅ 记忆已更新(`starnet-competitive-landscape-2026-06` 改写为"重立:记忆治理层" + MEMORY.md 索引)。
  2. ✅ 本块(实时进度顶部重立结论)已落。
  3. ✅ 回退点已建:git commit `ea74074`「重立前回退点」(只提了 01_docs 文档,未碰 graph.js/store.js 等代码 WIP)。
  4. ⏳ **文档返工 = 还没开始。** `星网_产品全局plan.md` 已读懂(确认通篇是旧"跨工具核心":§一痛点全是"散落各工具/换工具从头来"、北极星楼梯第②级=跨所有AI、§四护城河①=活注入——全要改);`星网_执行计划.md`、`参考文档/星网_记忆引擎设计.md` 尚未逐字读。
- **▶️ 下次第一件事:** 返工 `星网_产品全局plan.md`(锚,先它)——把核心从"跨工具记忆"换成"记忆治理层(看见+减负+归你)";跨工具/活注入降为捎带手;镜子+新陈代谢扶正为头牌;北极星楼梯/四步路线要按新核心重排(楼梯是创始人在意的视觉,改前先把新排法给他看)。改完 plan 再顺 执行计划 + 记忆引擎设计,然后才谈工程架构。

<details><summary>↓ 上一轮:记忆要做「工程架构」且包住记忆引擎设计(2026-06-09 早,仍有效,服务于上面重立后的产品)</summary>

**🏛️ 大决定:记忆要做「工程架构」,且工程架构包住记忆引擎设计(2026-06-09,创始人提出 · 优先级高,先读这块)**
- **缘起:** 讨论"工作记忆快层归②还是③"时,创始人意识到根问题——**星网的记忆没有一个好的工程架构**;这类问题都该归"记忆"统一管,不该被楼梯台阶切碎。
- **Claude 厘清:** 手上有「记忆引擎设计(认知模型=怎么想)✅」+「楼梯/执行计划(上货顺序)✅」,**缺第三样 = 记忆工程架构(怎么搭:零件+数据流+接口)**。一直拿设计文档当工程架构用,是缺口。"快层归②还是③"的别扭 = 拿竖的楼梯去切横的记忆系统的症状。
- **🔴 创始人定:工程架构 = 上层容器,《记忆引擎设计》并进去一起设计(是"包含",不是两份并排)。创始人尚未细读《记忆引擎设计》,之后要一起逐条过、讨论。**
- **零件草图(Claude 先粗描,待细化):** 池子 / 进料口(吸入归一化)/ 出料口(adapters+选片)/ 同步管子(归属三类)/ 新陈代谢(含快层)/ 关联网 / 查询口 / 镜子。数据流:各家→进料口→**池子**(代谢、关联在池子上加工)→出料口→各家;查询口+镜子从池子取。
- **下一步:** 正经起草「记忆工程架构」(把散落的记忆问题——快层/同步/镜子照什么——全收进来),排在动手做 ② 之前。**本轮先存进度,未开写。**
- **本轮定位纠正同步:** `星网_定位纠正_进行中.md` 已累积到 §十(供应链/注入=贴合机制非引擎/第一性重组/镜子照关系而非列内容)。**四条待拍已拍板=同意**(①活注入降级为出口、唯一护城河=池子;②plan"活注入引擎"改写成"同步管道+adapters";③记忆引擎"熄灭"→"变暗";④镜子按第一性重做"该照什么");第⑤条"快层归②还是③"搁置(由上面工程架构统一收编)。**这四条待写回 plan/记忆引擎,但排在工程架构讨论之后。**

</details>

<details><summary>↓ 上一轮:执行计划按北极星楼梯 v4 重排(2026-06-08,已落盘,留档)</summary>

**🪜 执行计划按北极星楼梯重排(2026-06-08,v4,创始人拍板"根据楼梯图重排"):治"文档混乱/被旧顺序牵制"。**
- **根因诊断(创始人认同):** 旧 v3 执行计划**违反了 plan 那张楼梯图**——楼梯是 ①一个AI懂你→②跨所有AI→③照镜子→④主动性,可旧计划把 ③(节点图/灵魂)做成"批次2"提前、把 ②(跨AI/接住记忆=创始人核心诉求)扔到"阶段二"靠后。**顺序反了,这就是"接住记忆难受地排在后头"的根。**
- **磁盘核实(诚实,不信文档自吹):** `~/.starnet/personal/` 只有 preferences 一档9条偏好(三档分层从没落代码);后端9文件全是MCP server(无导入器/无代谢引擎);graph.js 在但分区/活跃度是 priority **假装**的=空壳。**号称"进批次2"实际只盖了个没东西可装的橱窗。创始人"完成度没达标"判断成立且更实。**
- **✅ 重排已落盘:** `星网_执行计划.md` 重写为 **v4 北极星楼梯版**,铁律=**批次顺序=楼梯级数 ①→②→③→④谁都不许插队**。新四级:**①一个AI懂你**(✅功能通 + 补债:1·B-1三档分层落地、1·B-2注入解耦落代码=②硬前置)→**②跨所有AI·里子主体·本阶段重点**(主线=2-1导入器接住19条Claude记忆按§13·2加档判据分档,创始人拍板先做;其后2-2 adapters/2-3同步)→**③照镜子·脸**(节点图新陈代谢life驱动壳变真/时序连边/工作记忆快层/双向同步/hooks高亮,候选池待逐级定序)→**④主动性**(睡眠期半自动蒸馏)。
- **核心改动:** 把②跨AI/导入器从旧"阶段二"整个提前到③节点图之前=**先里子后脸**(创始人认同)。①补两笔旧债(分层+注入解耦)作②前置。
- **🟡 留的开口(创始人定):** 里子(②)和脸(③)**内部任务级拆解**留"待逐级讨论",没这轮定死——下一步开工前逐项和创始人细化(如导入器怎么拆、工作记忆快层归②还是③)。
- **下一步:** 进①级补债——先 1·B-1 三档分层落地(导入器的容器),再 1·B-2 注入解耦落代码,然后 ②级 2-1 导入器。

</details>

<details><summary>↓ 上一轮:档模型+注入解耦写进引擎文档(2026-06-08,已落盘,留档)</summary>

**✅ 档模型 + 注入解耦 = 已写进引擎文档(2026-06-08 续,创始人拍板"开干"→已落盘,导入器准绳就位):**
- **状态:** 两条已正式写进《参考文档/星网_记忆引擎设计.md》,六处改动:头部修订标记、§2(content-kind 只管代谢)、§5·2(删"进活注入?"那列病根)、**新增 §7·0 注入资格解耦(核心)**、§7·1/7·2 改吃普适+相关打分、§十三·2 加档判据 + 四类候选走查、§十二红线 +2 条。
- **落地要点(写代码照这两条):** ①注入解耦两根正交轴——轴①`content-kind` 只管代谢,轴②注入资格由"普适度 universality × 当前相关度 relevance"打分;L1 取普适分高的(偏好+身份恒常项,不分档),L2 普适+相关合分+扩散激活(资料该蹦就蹦)。**接法:把 store.js 现有"按 content-kind 一刀切过滤"删掉,改全档进打分池;universality 无字段时退化用 priority/always 保底——暂不新增宪法字段,零返工。** ②加档判据:新档独立⟺要别的档给不了的代谢尺子或注入行为;按此筛=工作记忆已够格、目标档待定单独评、人物/洞察用标签+links 不成档。
- **下一步(导入器,准绳已就位):** 写导入器,照加档判据把19条Claude记忆分档(偏好/身份恒常→活注入池靠普适分;项目知识/资料→只归你保管,L2 凭相关蹦)。

<details><summary>↓ 原待拍记录(2026-06-08,已拍板落盘,留档)</summary>

**🧱 档模型 + 注入解耦 = 导入器前置,待创始人拍是否写进引擎文档(2026-06-08 续,设计判断已给/未落盘 ← 下次接续核心):**
- **缘起:** 创始人质疑记忆引擎的"三层抽屉(偏好/档案/资料)"——①覆盖不全 ②读取时层级优先级分得不够好。Claude 通读《记忆引擎设计》后给判断,**两点都成立,且第②点更要害**。
- **① 覆盖:** 文档其实已偷加第四种 `state`(工作记忆),"三层"只是给用户看的简化(§5.2 已是四种 content-kind)。**还缺/塞着别扭的:目标计划、人物关系、技能流程、蒸馏洞察。** 但**判据钉死(防无脑加档):"一个新档值得独立,当且仅当它需要别的档给不了的代谢尺子或注入行为。"** 按此筛:工作记忆够格、目标(达成即归档+要提醒)可能够格;人物/洞察大概率用标签+links 就够,不单独成档。
- **② 注入优先级(真要害):** 现在注入是**按"档"一刀切硬开关**(偏好✅进 / 档案❌ / 资料❌,见 §5.2 末列)。两个毛病:**(a) 身份恒常类被误杀**("你是创始人"在 event 档,按规矩不注入,可它像偏好一样永远相关);**(b) L1 启动注入手里有语境**(项目路径 + UserPromptSubmit 拿到的你刚说那句话)**却只喂给偏好打分**,资料/档案享受不到——你正搞苹果ID时那条资料该蹦却被档挡死。
- **🔧 Claude 给的解法(核心):解耦两根正交轴。** 轴①`content-kind` 只管**怎么代谢**(衰减/内化);轴②**注入资格** 不由档定、由"普适度 × 当前相关度"打分定。**L1(无query)放普适分高的——不分档**(偏好+身份恒常项都进);**L2(有query/语境)全档按相关度打分+扩散激活**,资料该蹦就蹦。一刀解耦,两个毛病同时消。
- **⛓️ 关键连接:这是导入器的前置。** 导入器干的第一件事就是给19条记忆**分档**;档模型没捶对,导进去就是错分类、以后返工。**所以下次别急着写导入器,先把"档模型修正 + 注入解耦"定下来。**
- **🟡 待拍(下次第一件事):** 要不要把上面两条(注入解耦 + 加档判据)正式写进《星网_记忆引擎设计.md》作为这层的修正?写进去导入器才有准绳。**创始人尚未答。**

</details>

</details>

**🧭 认知校准 + 当前进度定位(2026-06-08 续):**
- **创始人自我校准(重要,已确认):** 意识到"我以为星网已有的东西,客观上其实没有"。Claude 给了"图纸 vs 楼"框架——**设计(想法)已画到顶楼,代码只盖到①②级**。对照表:**以为有其实没有** = 接住Claude记忆/档案资料抽屉/节点图新陈代谢/Cursor-Codex/睡眠;**客观真有** = 偏好链路(存→注入→按任务挑,实测通)/节点图壳/9条偏好真落盘。**约定:Claude 以后汇报默认分"图纸画到哪 / 楼盖到哪"两栏,判断"有没有X"看代码不看文档。**
- **进度定位(精确):** 站在**阶段一·批次2 刚开头**(2-① 节点图✅,余 2-②双向同步/2-②·5工作记忆快层/2-③新陈代谢/2-④hooks高亮 全没动);批次1主柱链全✅。创始人最在意的"接住Claude记忆"=楼梯**第②级/阶段二**,按计划还没到,所以现在没接住是正常、不是坏了。
- **下一步建议(Claude 已给判断/未拍板):修里子不修脸 = 做"建档案/资料抽屉 + 导入器"**,把19条Claude记忆按档分进来(偏好类进活注入、档案资料类只归你保管不乱喂AI)。**否决了**工作记忆快层(单工具用户太早)和节点图新陈代谢(修脸、里子还空)。**注意:此建议的前提是上面"档模型修正"先定——两件事连在一起。**
- **现状实测数据(本次核查):** 星网 `~/.starnet/personal/preferences/` 真有 **9 条偏好**(手动种子+pytest,非导入);Claude auto-memory `.claude/.../memory/` 有 **19 个 .md**(丰富得多);后端6个js全是MCP server,**无导入器**。

**🪜 楼梯图钉为 plan 北极星 + plan 对齐审计(2026-06-08,✅已全部落盘):**
- **节点图分层 → 先放着(创始人拍板)。** 看了双主题渲染(白冰原/深空星云):核心区中心亮、休眠"别纠正错别字"被甩外圈变暗,结构对了,但 9 条点撑不出三道清楚的环(中间活跃圈凑不齐)。结论:**这图为"几十上百条记忆"设计,9 条本就稀——等"上行吸纳"把 Claude/Codex 记忆汇进来点变多,自然成环,现在不调旋钮。**
- **判决:不动整个 plan 计划。** 创始人问"破烂的中心问题要不要改整个计划"——分两层看:**战略层(plan)中心没病**(四步楼梯/三部件/两护城河清楚),**呈现层(节点图)散是执行层小事**。真实缺的不是改 plan,是"中心"没被一眼看见的样子摆出来。
- **拍板:把那张"楼梯图"钉到全局 plan 最开头当北极星视觉。**(`①一个AI懂你✅你在这 → ②跨所有AI → ③会照镜子 → ④主动性 → 顶=你自己的贾维斯,归你`;配"你不是在迷路,你在爬这座楼梯,楼梯顶一直没动"。)内容本就在 plan 第六节四步表里,提到开门第一眼,治创始人"一打开觉得乱/没中心"的不爽。
- **连带要求:plan 文档集对齐审计** —— 确保所有 plan 文件相互对齐、不被旧观念污染、保持简洁。**✅ 已全套复审(7+份,2026-06-08,4个agent并行逐字比对北极星):全局plan/执行计划/记忆引擎设计/数据格式宪法/决策清单/1-①实现方案/地基层复盘 全部对齐良好——无硬矛盾、无旧"主柱+皮肤"污染、记忆引擎新字段(content-kind/life/links/derived-from)已追加进数据宪法§5.5。**(一个agent误报"地基层复盘定义矛盾",已甄别:三层模型里"地基=跨工具同步"本就正确,不改。)
- **✅ 状态(2026-06-08 已落盘):** ①楼梯图已钉进全局plan顶部(新增「北极星·你在爬一座楼梯」节,①一个AI懂你✅你在这→②跨所有AI→③会照镜子→④主动性→顶=贾维斯归你)②旧措辞已清(5个文件头"全套五份文档已同步"过时计数删除,实时进度头部日期对齐到06/08)③**两套分层叫法已加Rosetta对照**(全局plan三部件那块:身体=主柱/脸=灵魂/神经=活注入单拎/地基=跨工具同步归阶段②;创始人拍板"两套都留,加一行对照")。**北极星视觉+术语统一到位,治"一打开觉得乱/没中心"。**

**🔧 节点图分层 + 注入三补(2026-06-08,代码已改+已验证):**
- **节点图:** `graph.js` 从"平铺散点(向日葵螺旋按录入顺序)"改成**按活跃度同心区**——核心区中心最亮/活跃中圈/休眠外圈变暗。**v1 用 priority 当"活跃度"替身**(high=核心/medium=活跃/low=休眠;真实9条=5核心/3活跃/1休眠,三圈分明),批次2接 `life` 数据后只改 `zoneOf` 一行就变真。两主题都吃 `glow`。语法+落点模拟已验,**像素需 `npx tauri dev` 眼睛确认**。治了创始人"散的、没层级"的直观不爽。
- **注入三补**(`store.js` + 新 `user-prompt-hook.js`):**A 看你说的话注入** = 新增 UserPromptSubmit hook,按你刚说那句话挑相关偏好补注,补 SessionStart 只看项目语言的盲区(已挂 settings.json、端到端验证:"做产品决策"顶出决策偏好、"聊天气"闭嘴)。**B 全局钉子** = `always:true` 永远注入不被挤出(机制+字段已落,向后兼容;**待标记**具体偏好才生效)。**C** 把过时的"决策三表"对齐到代码实际(优先级保底+语境匹配)。
- **⚠️ A 的已知短板(诚实):** 语境匹配是关键词子串比对,对标签好的偏好准,但**中文措辞变体会漏**("写测试"命中、"写个测试"漏掉,差一个"个")。鲁棒匹配 = 批次2 语义匹配(向量)+ 批次3 自动补中文标签。**注入核心病(只看项目语言、不看你的话)已治,但匹配精度待引擎层补。**

**🧠 记忆引擎升级写进 plan(2026-06-08):从"分类仓库"升到"会睡觉的大脑"。** 起点 = 创始人新文件 `星网_今晚讨论纪要_记忆模型与镜子定位.md`(双维度原子 + 关联网 + 镜子定位);Claude 对标了最新脑科学与 2025-2026 记忆系统(A-MEM/Zep/Letta sleep-time/Anthropic memory tool)核实后,**往原三层骨架上叠四个机制**(不推翻):①**睡眠期巩固**(把蒸馏+代谢+"二"合成一个后台引擎,空闲回放→蒸馏派生记忆→代谢,对标 Letta sleep-time + 睡眠巩固脑科学)②**双速记忆 CLS**(补"工作记忆快层"=最大缺口,跨工具接上下文,最实用优先)③**关联网升级**(真关系+双时序+人确认,替换共享标签连边)④**框架:Claude=海马、星网=新皮层+睡眠**(互补不替代)。**两条战略红线写死:不卷记忆模型先进度只卷"镜子+归属";复杂藏引擎、极简留脸。** 落点:plan 新增 §5·6、宪法新增 §5·5(记忆原子追加字段,B-分阶段)、执行计划批次2/3 折入(工作记忆快层=新 `2-②·5` 优先)、**新建工程规格 `参考文档/星网_记忆引擎设计.md`(公式/睡眠期流程/扩散激活/与 store.js·graph.js 接口/分阶段路线图,写代码照这份)**。**关键诚实:~70% 料来自纪要自己点名的缺口,~30% 是研究新填;唯一替创始人做的调和判断 = 纪要"推翻三层" vs 老plan"供着三层"的矛盾 → 三层当"视图",双维度原子当"底层存储",并存。** 详见 memory [[starnet-layering-system]]。

**✅ 节点图升级为「双主题」系统(2026-06-06,commit `4433627`,已实现+已存档):** 创始人拍板把节点图做成两套可切换主题——**主题一 白冰原(ice)** = 失重旋转感落地(整团星座刚体慢转~4min/圈、碰到节点缓停、标签恒正、白色冰原巨物缓转背景、深色反差节点带落影、当前节点青色神经元高亮);**主题二 深空星云(nebula)** = 上一版(深空星云+漫天星点+银灰发光节点+神经纤维脉冲)。`graph.js` 重构为「共用引擎 + 两套主题」,右上角按钮一键切、localStorage 记忆、节点颜色绘制时按主题取(切换瞬间生效不重建图)。两套都还留足调整空间(配色/浓淡/转速/高亮强弱)。回退点:`bc36761`(纯星云黑底)。详见 memory [[starnet-nodegraph-visual-direction]]。

**🔧 踩坑记档(2026-06-06):换机/重编 Tauri 撞「页面文件太小」(OS error 1455)。** 根因 = 这台机器**提交内存额度(commit)被占满**(当时仅剩 1.4G;`windows` 这个巨型 crate 单编要好几个 G),而 **Windows 页面文件设得才 0.2G、太小**,撑不开提交天花板。临时解:①关 Chrome/微信腾额度 ②`CARGO_BUILD_JOBS=1` 单线程编(峰值需求压到最低)→ 编过。**根治待办(创始人已认、择日做):把页面文件调大(系统托管或固定 16~32G),需重启一次生效。** 与代码无关,纯环境/系统配置。

**🛰️ 失重旋转感来源(2026-06-06,已被上面"双主题"落地):** 创始人给参考图=《星际穿越》Endurance 环站在冰原上方缓缓自转(`C:\Users\ghqah\OneDrive\图片\屏幕快照\屏幕截图 2026-06-06 164216.png`),爱那个失重感。已拆解:失重感=①整团极慢刚体自转(命根子,整座星座作为整体绕中心 3~5 分钟转一圈,节点保持相对位置)②背景视差纵深(星云/星点不同速度漂移,显得节点浮在巨物之上)③一切慢+带惯性、绝不卡顿。**待拍2点:(1)自转取舍**——我的方案=平时慢转+鼠标一碰自动缓停+标签恒正(兼顾好看与能用)vs 一直转不停;**(2)背景**——保持深空 vs 换成"有纹理、缓动的巨大表面"(深色版星云大陆,不一定白)。两点定了就动手。详见 memory [[starnet-nodegraph-visual-direction]]。

**✅ 节点图视觉(银灰/星云/神经纤维)已落地(2026-06-06,commit 31958e5):** 银灰冷调发光节点 + 重做的深空星云背景(径向底+冷调云团+胶片颗粒+暗角,去廉价感)+ 连线=发光神经纤维(一道线性渐变亮带沿曲线平滑流过,已按创始人反馈去掉离散小光点)+ 点节点开编辑器。已重启 Tauri 到此版。

**✅ 节点图视觉升级 已实现(2026-06-06):星座×神经元 + Interstellar 星云美学。** **关键技术决策:砍掉 Cytoscape,改手写 Canvas 2D 渲染器**(Cytoscape 撑不动光晕/星云/脉冲/失重,粒子库又绑不住"节点=真实偏好"的数据)。`graph.js` 全重写:深空星云背景(暖金/冷青去饱和,离屏预渲染)+ 漫天星点闪烁 + 发光光晕节点(向日葵螺旋布局+autofit居中,失重正弦漂浮)+ 共享标签连线 + 沿线脉冲光点 + 点节点开编辑器(命中容错放大)。已移除 cytoscape 依赖。**验证:Chrome 网页版实测——9节点居中成星座、点节点正确跳编辑器加载对应偏好,全链路通**(Tauri 截图因高DPI+前台锁噪音大,改用浏览器验证才看清,布局一直是对的)。已重启 Tauri 到最终版。**下一步可调(交创始人定):星云浓淡、配色、脉冲频率、是否加节点拖动惯性等观感微调;再往下是 2-③ 新陈代谢(亮度=活跃度的钩子已埋)。**
> 以下为该方向定稿时的原始记录(2026-06-06):
**定下的方向(创始人原话):**
- **星座 × 神经元 混合体**(不是二选一,是融合)
- **节点带光晕**(glow/halo),失重**动态漂浮**(weightless drift,节点缓慢飘动不是钉死)
- **连接线有神经元脉冲**(synaptic pulse 沿边流动的光点)
- **背景是浩瀚星云**(nebula,深空)
- **灵感参考 = Interstellar 电影片段**:`C:\Users\ghqah\Videos\屏幕录制\屏幕录制 2026-05-01 111526.mp4`(已抽帧确认是《星际穿越》:Endurance环站旋转、飞船引擎微光、Gargantua黑洞金色吸积盘/虫洞星云)。**提炼的美学:** 深邃纯黑空间 + 暖金/冷青的星云(去饱和、电影感非艳丽)+ 微小发光点失重漂浮(尺度感+静默)+ 柔光 bloom + 胶片颗粒/轻微暗角。
- **和新陈代谢(2-③)的接口**:发光亮度天然承载"点亮/熄灭"——活跃=亮、休眠=暗,视觉系统现在就按这套设计,别到时候补。
- **下一步(未做):** 在 graph.js / style.css 上实现这套——星云背景层、节点光晕+漂浮动画、边上脉冲粒子。Cytoscape 够不够撑这种效果待评估(可能要 canvas 叠层或换/补渲染);开工先扫一下实现路径(纯JS粒子/着色器 vs Cytoscape扩展)。**当前代码=朴素版,已提交 feat/2-1-node-graph,视觉升级是新一轮。**

**✅ 批次 2-① 节点图 已落地(2026-06-06):第一次能"打开自己的大脑"。** 按铁律B扫实时生态(周下载:Cytoscape.js ~500K > vis-network ~200K > Sigma.js ~50K),**锁 Cytoscape.js 3.34.0**(零依赖/原生vanilla/内置力导向cose/我们几十节点的规模 Sigma 的 WebGL 是杀鸡牛刀)。实现:`02_frontend/graph.js` 读 `Storage.list()` → 每条偏好=节点(颜色按类型/大小按优先级)、**共享标签的偏好之间连边**(网长出来不是散点)、力导向布局、**点节点=切回编辑器打开那一条**;index.html 加「节点图/编辑器」双视图切换(节点图为默认、深色画布营造空间感);Cytoscape vendored 进 `vendor/`、sync-dist 已带上。**折入完成:导入 A 档 8 条偏好**(`06_scripts/seed-8-preferences.js`,幂等,用宪法格式 API 生成)→ 现 `~/.starnet/personal/preferences/` 共 **9 条**,图上能看见"沟通风格"簇。**验证:** 语法check全过;Node 模拟DOM/cytoscape/Storage 实跑 graph.js = 9节点/7边(完全符合预期)、点击回调传对id、6断言全过;后端store测试 19/19 不受影响。**未亲眼看渲染**(Tauri GUI 没法headless看)——下次 `cd 02_frontend && npm run dev` 启 Tauri 一眼确认视觉。
**2-① 下一步(批次2余下):** 2-② 节点-文件双向同步 / 2-③ 节点新陈代谢(熄灭/点亮/内化/锁定,甩开Obsidian的命门)/ 2-④ Claude Code hooks 节点高亮。另:点节点目前是"应用内编辑器打开",真·打开OS文件留作可选增强。

**方向定稿(2026-06-06):星网边界升维——"管偏好/记忆" → "+ 工程文件聚合呈现治理"。** 创始人亲历文件散落(代理配置散 5 处+4 份进度文档漂移),澄清核心愿景:agent 产生的工程文件**不搬**(留原地),但**传到星网聚合呈现、随时看见+改动**。关键认知:这正是数据格式宪法里**早已定义、零实装的 `projects/` 层**(任意类型/留真实目录/nodes.json 索引/filters 排除构建产物)——不是新功能,是兑现搁置蓝图。**本次只定稿+排路线图,不写码**(projects 地基并入批次 2;先按节奏做批次 2)。已写进 plan §十二 + 执行计划批次2 + 新 memory `starnet-projects-layer-scope`(含 5 条待决子问题)。**另:本次把执行计划从 5/30 冻结态补同步到 v3(批次1标完成、分层/上行吸纳排入)。**

**✅ 下一步已定(2026-06-06):正式开做批次 2(选 A,非手动导入 B)。首块 = `2-① 节点图`先行**(不先做分层)——理由:节点图是产品的"脸"/灵魂、看得见给劲,先拿到"打开自己大脑"那一下续命;分层(2-⓪)/新陈代谢(2-③)往上叠,非硬依赖。**折入** B 的价值:做节点图时把导入地图 A 档 8 条偏好导进来,作为节点亮在图上(一次拿到"更多偏好自动加载"+"第一次看见大脑")。开工三下:①按铁律B扫生态定图库(候选 Cytoscape.js)②渲染 `~/.starnet/personal/` 现有偏好(+8条)成节点图 ③点节点=打开对应 .md。

**这次对话(2026-06-04):一口气做完 1-②+1-③,批次 1 主柱链条全部跑通。** 1-② MCP server(recall按需查)+ 1-③ 活注入引擎(hook自动加载 + 三层智能打分按语境挑)。用户已实测自动加载生效("牛逼啊已经可以了")。Claude Code 这一家:存偏好→AI开口就懂你→按当前在干的活智能挑。下一站批次 2 灵魂层(节点图,护城河②)。**注意用户核心诉求是"任何agent都自动加载"(见 memory starnet-autoload-is-core),跨agent靠 adapters,优先级可能要从阶段二提前——他若提,认真排。**

**定位深聊(2026-06-04 同日稍后):创始人亲手搭了一套 Claude+Codex 跨工具记忆体系,反推地基层需求 + 夯实定位。** 产出三样:① **7 步手搭复盘** = 地基层需求来源(新文件 `地基层_需求来源_创始人手搭复盘.md`)② **定位战略内核**(新 memory `starnet-positioning-sovereignty`:前提假设对决 / 聚合主权 / 1Password 类比;**"对手是黑箱"论据已被核查证伪**——Claude/Codex 记忆其实都有明文,差异化靠**聚合归属 + 大众可掌控**,不靠"可见")③ **可见性二段定义**(技术可见 vs 大众可掌控,已并入 plan 护城河②)。
- **判决:今天产出 ≈95% 是补充/深化(夯实护城河②,plan v3.0 骨架经住深挖没被推翻);≈5% 是新需求 = "上行吸纳工具自带记忆"(待排,阶段二地基)。** plan 第七节护城河②、地基层均已补。
- **和上面那条 autoload 提前呼应:** 聚合主权论证给"adapters/跨agent 优先级提前"提供了战略钢筋(对手结构上不会做聚合层,这块越早占越好)——他若推进跨工具,这是加分理由。

**记忆分层体系定稿(2026-06-05):** 几轮深聊把星网分层骨架捶定——**三档主轴**(偏好/档案/资料)+ 作用域当标签;**减法全星网统一**(暗淡不删,只"被推翻"才删;判推翻甩给用户+agent,星网不当裁判);**星网有活跃度**(主动把垃圾拎出来逼用户整理,真删用户拍板 = 从"仓库"升级"逼你自我迭代的镜子");**核心一份 + 出口贴合各工具**(解"多元↔极简"矛盾)。已写进 plan 第五·五节 + 新 memory `starnet-layering-system`。**下一步:这套是冷启动导入(Claude auto-memory→星网)的分层依据,导入器照三档打标签**(A个人偏好进活注入池、C项目知识/D资料不进,见前文导入地图)。

**📍 当前落点 + 待决策(2026-06-05,保存进度):** 这两天对话"设计"跑得远(定位/分层/导入想法),"代码"还停在批次1(只有偏好链路、只 Claude Code 一家)。**要把 Claude auto-memory 导入星网,缺两样:① 导入器(读Claude记忆→按三档分类→补字段→转格式→落盘,没写)② 刚定稿的分层(档案层/资料层/节点图/新陈代谢)还没变成代码——`~/.starnet` 现在只有 `preferences/` 一个目录 + 那条 pytest。** 结论:现在硬导也只是多几条躺 preferences 里,分层/代谢看不见(承载设计的"身体"=批次2 还没建)。设计领先实现一大截,这不是坏事,但要认清"不是差最后一步导入,是星网主体还没建"。
**下一步二选一(✅ 2026-06-06 已拍=A):** **A·正经做**——推批次2(节点图+分层+新陈代谢),身体建好导入才有意义;~~B·先尝鲜手动导入~~(否决:会喂"只设计不动手"惯性)。**已定 A,首块 2-① 节点图先行,折入 8 条导入,详见顶部"下一步已定"。**
**导入地图(A档8条,下次导入依据):** user-role 拆6条(①大白话直接给判断②技术任务AI主动扛只抛关键决策③可直接指错但要客观、拒无依据悲观与献媚④创业者视角非评估师⑤错别字别纠正、中文混英术语⑥环境Win11双盘+PowerShell/Bash)+ no-begging 1条(用客观痛点+能力指标判断,不靠讨好/用户数)+ autoload 1条(偏好要任何agent自动加载)。**不进活注入**:B档案(harness超体课)、C项目知识(product-core/positioning/marketing-redlines)、D资料(progress/anti-ban/weixin)。

### 0. 1-② MCP server —— ✅ 写完,端到端验证通过,已提交

- **位置:** `03_backend/mcp-server/`(后端,空目录里新建)。7 文件:`index.js`(MCP 入口)、`store.js`(读 ~/.starnet + 召回逻辑)、`test.js`(store 冒烟测试)、`handshake-check.js`(MCP 端到端握手)、`package.json`/`package-lock.json`/`README.md`。
- **选型(铁律B 扫生态定的):** 官方 `@modelcontextprotocol/sdk` v1.29(当下最优解,6600万下载 Tier1)+ stdio 传输(Claude Code 当子进程拉起)+ 纯 Node JS。**直接复用 `02_frontend/starnet-format.js`** 解析,单一权威不复制。日志走 stderr(stdout 留给协议)。
- **两个工具:** `recall(query,limit?)` 按需查(标题>标签>场景>正文 加权打分)+ `get_active_preferences(limit?)` 会话开头注入(按 priority 笨排序前 ≤5)。applies-to 过滤已生效(空=全适用)。
- **范围边界(本次拍板):** 1-② 只"通水管",**三层叠加智能打分不做,留给 1-③**(护城河①的命,单独打磨,别让 1-② 把它掏空)。
- **验证:** store 冒烟测试 11/11 过(含坏文件跳过不崩、applies-to 过滤、笨排序);MCP 端到端握手 4/4 过(独立 client 拉起子进程→initialize→listTools→真调两个工具);对真实那条 pytest 偏好实跑命中正确。
- **接入方式(写进 README):** `claude mcp add starnet -- node "<index.js 绝对路径>"` 或写 `.mcp.json`。
- **待办(下次接续第一件事):** 接进 Claude Code 实测(`claude mcp add` + `/mcp` 看到),然后进 1-③ 活注入引擎。

### 0老. 1-① 偏好编辑器 —— ✅ 代码写完,验证通过,已提交(上一轮)

- **位置:** `02_frontend/`,5 个文件:
  - `starnet-format.js` —— 数据格式宪法的代码实现(id/slug 生成、frontmatter 拼装/解析、字段顺序锁死、默认值、updated 刷新)。**后面所有批次复用这块地基。**
  - `storage.js` —— 存储层隔离。网页版用 localStorage;套 Tauri 时**只换这一个文件**为真写 `~/.starnet/personal/`,界面零改动。
  - `index.html` + `style.css` + `app.js` —— 左列表右表单,新建/编辑/删除/导出 .md。
- **Tauri 决策:先纯网页版**(localStorage 跑通逻辑)→ 下一步套 Tauri 落盘。存储层已隔离,换壳不返工。
- **验证:** Node 测 format+storage 往返(改 title 保留 uuid、删除走 trash、坏数据不崩);真浏览器实操填表保存,亲眼确认存进 localStorage 的 markdown **字段顺序与数据格式宪法第六节示例完全一致**。验收 6 条全过。
- **提交:** `feat/1-1-preference-editor` 分支,commit `008a94a`,21 文件,1977 行新增。启动脚本删除已确认,文档集 v3 重组一并入库。
- **待办(下次接续第一件事):** 套 Tauri 壳收尾 1-①(只换 storage.js 落盘路径),然后进 1-② MCP server。

### 0b. 选型铁律(已写进执行计划顶部"选型铁律"节)

- **规矩 A:** 全程纯 HTML/CSS/JS,不引前端框架。批次 2 节点图用纯 JS 图库(**候选 Cytoscape.js**,不锁死,批次 2 开工前最终定)。**原 2-① 的 React Flow 已松绑改写。**
- **规矩 B:** 库与 GitHub 借鉴对象都是"当下最优解"不锁死;每个批次**开工那一刻**扫实时生态找最强借鉴,定了就动手;**做到一半不回头重选库**(中途发现更好的进 `04_IDEA_INBOX`)。借鉴=学思路不无脑抄,守住简单+本地化。
- 配套:`星网_1-①实现方案.md`。

---

## 上一轮对话结论(已落地,留档)

**三个待拍问题全部拍板 + 产品重构为三层模型 + 三份 plan 全部重写完成(plan→v3.0,执行计划→v2)。定位锁定。**

### 1. 三个待拍问题 —— 全拍完了

1. **跨工具同步降级成地基** —— ✅ **认了。** 不跟 AGENTS.md 标准拼同步,抄开源当水管,力气全砸两条护城河。
2. **蒸馏放哪** —— ✅ **定为产品灵魂 + 半自动起步。** 灵魂方向="越用越像你";阶段一只做半自动(观察→建议→一键确认),全自动留阶段四。理由:自动蒸馏做不准=镜子歪,比没有还糟,崩信任。
3. **节点图** —— ✅ **保留并升级为"灵魂层的脸"。** 命门是新陈代谢(熄灭/点亮/内化/锁定),这是甩开 Obsidian 的结构性差别,不是装饰皮。

### 2. 产品重构为三层模型(取代旧"主柱+皮肤")

```
灵魂(脸)  = 越用越像你的大脑 = 那张节点图   ← 护城河②
            观察→蒸馏(半自动)→新陈代谢
主柱(身体)= 个人偏好的家(跟人走+跨工具+markdown主权)  ← 价值本体
地基(水管)= 跨工具同步(抄开源,不自造)        ← 必需非卖点
```

**两条护城河(力气全砸):** ①**活注入**(按当前任务实时挑偏好,非死文件)②**vibecoder 体验**(节点图新陈代谢+大白话)。

### 3. 四件"老愿景残留"判决(防止又当功能做)

| 想法 | 机制 | 卖点 | 阶段一立项 |
|---|---|---|---|
| 节点=文件编辑 | ✅留(灵魂底层) | ✅好处句式,不喊"替代文件树" | ✅节点图必备 |
| 防漂移污染 | — | ✅好处句式,❌不喊"防止" | ❌活注入副产品 |
| 省token | — | ✅好处句式,❌不当主卖点 | ❌活注入副产品 |
| skill成资产 | ✅留(主柱延伸) | ✅"你的资产换工具不丢" | ❌阶段三接入 |

纪律:任何想法先问"从三层哪层长出来"。长不出的不立项。

### 4. 营销新铁律:好处句式 vs 承诺句式

**说"好处"能当卖点,说"承诺"踩雷。** 动词锁死:`帮/让/顺带`=好处(能喊),`防止/避免/替代/消灭`=承诺(禁用,兑现不了被拆穿)。详见 memory starnet-marketing-redlines。

### 5. 已拍板的工作方式(不可回退)

- **禁止要饭式讨好用户**:门槛用能力指标不用人数,不写"先丑后美/挑眼睛亮的人"。痛点客观存在,不验证有没有人疼。详见 memory starnet-no-begging-mindset。

---

## 一、当前进度位置

🟢 **阶段一 · 批次 1 主柱链条全部完成;批次 2 灵魂层已起步(2-① 节点图✅,余 2-②/③/④)**

```
✅ 0-①  数据格式宪法                 (参考文档/星网_数据格式宪法.md)
✅ 0-②  产品决策清单                 (参考文档/星网_产品决策清单.md)
✅ 0-③  用户画像 → 并入 plan 第二节,免单写(创始人本人即目标用户)

—— 批次 1 主柱链条(最高优先级,先跑通 demo)——
✅  1-①  个人偏好编辑器               ← 网页+Tauri桌面双完成,已提交
✅  1-②  MCP server 框架               ← 写完+双重验证通过+已提交(只通水管,三层打分留1-③)
✅  1-③  活注入引擎(护城河①)  ← 主动注入(hook自动加载)+三层智能打分 双完成,语境不串台

🎉 批次 1 主柱链条全部跑通(存→取→自动加载→按语境智能挑)。下一站批次 2 灵魂层。

—— 批次 2 灵魂层:节点图 + 新陈代谢(护城河②)——
✅  2-①  节点图渲染(Cytoscape.js 3.34,共享标签连边,点节点开编辑器,折入8条导入)
□  2-②  节点-文件双向同步(监听+新建)
□  2-③  节点新陈代谢(熄灭/点亮/内化/锁定)← 甩开 Obsidian 的命门
□  2-④  Claude Code hooks + 节点高亮

—— 批次 3 半自动蒸馏 ——
□  3-①  半自动蒸馏建议(观察→建议→一键确认)

—— 批次 4 创始人 dogfood ——
□  4-①  自己用自己的产品,判断有没有解决你的痛点
```

> **编号读法:`批次-序号`,序号即批次内执行顺序。旧的乱序编号(#7/#4a 等)已全部退役。**

**1-③ 进展(2026-06-04):** 拆两半,**先打通"主动注入"(用户核心诉求=自动加载,见 memory starnet-autoload-is-core)**:
- 新增 `03_backend/mcp-server/session-start-hook.js`:Claude Code SessionStart hook,开会话自动调 getActive 取≤5条→按契约 `hookSpecificOutput.additionalContext` 注入上下文,异常静默不卡会话。
- 已挂进 user settings.json(`%USERPROFILE%\.claude\settings.json`)的 hooks.SessionStart,matcher=startup+clear。已验证:脚本输出合法契约JSON含真实pytest偏好;settings.json JSON合法、旧配置全保留。
- **待用户验证:** 新开 Claude Code 会话,啥都不调,AI 应一上来就自带"用pytest写测试"偏好。
- 仅打通 **Claude Code 一家**;Cursor/Codex 自动加载=adapters(阶段二)。

**1-③ 三层打分完成(2026-06-04):** `store.js` 新增 `scoreRecord`(三层叠加:priority保底+context语境匹配+tags加分+title小补)和 `detectSignals(cwd)`(探测项目语言:pyproject/.py→python、package.json→node+读依赖认react/vue/tauri等)。`getActive` 改为接收 signals 用三层打分排序(无signals退化priority笨排序,向后兼容);`recall` 复用 scoreRecord(带body查正文,要求matched>0)。hook 改为读 stdin 的 cwd→探测语境→喂打分。
- **关键洞察(写进memory):** 数据宪法里 `applies-to`=哪些agent(非"项目路径"),与产品决策清单旧语义打架,按宪法走;"项目路径/文件类型"两层在真实数据里都落到 `context`/`tags`,合一为"语境匹配"。真实偏好常不填 context(用户不爱填字段),实际靠 tags+title 匹配——这正是批次3半自动蒸馏要补的(自动建议context/tags)。
- **验证:** test.js 19/19(含"React语境下react偏好顶过high优先级pytest、不串台");hook 对真实 02_frontend 探测出 node/javascript/tauri 并注入。

**下一个动作:** 进 **批次 2 灵魂层**(护城河②)。先 2-① 节点图渲染(纯JS图库,开工那刻扫生态最终定,候选Cytoscape.js)。开批次2前按选型铁律B重新扫一遍实时生态。

---

## 二、产品定位(锚点,不可漂移)

> **星网是给"用 AI 干活的普通人(vibecoder)"造的本地大脑——记住你怎么做事,让每个 AI 都照着"你"来干活,用得越久越像你。**

**三层模型(取代旧"主柱+皮肤"):地基(同步水管)/ 主柱(偏好身体)/ 灵魂(节点图脸)。**

**定位演变:**
- ① 早期两柱(看见+沉淀)→ 承认"看见 LLM 思考"做不到
- ② v2.x:一主柱(沉淀)+ 一皮肤(节点图)
- ③ **v3.0 本次:收窄 vibecoder,跨工具同步降级为地基,护城河转"活注入+普通人体验",蒸馏升为灵魂(半自动起步),节点图升为灵魂的脸,命门=新陈代谢。**

---

## 三、已做的关键决策(写代码前必查)

### 数据格式(详见 `参考文档/星网_数据格式宪法.md`)
- personal/ 分子目录(preferences/habits/decisions/diary)
- id 规则 `<type>-<slug>-<short-uuid>`,id ↔ 文件名双向锁死,以文件系统为准
- 字段只能追加不能改;7 个核心 frontmatter 字段

### 三个产品决策(详见 `参考文档/星网_产品决策清单.md`)
- **注入** = 混合分层:≤5 条主动注入 + recall 按需查
- **冲突** = 标记不仲裁,先比 applies-to 排假冲突,真冲突问用户
- **激活** = 三层叠加打分(全局高优先级 / 项目路径 / 文件类型),手动 tag 仅加分项

### 节点新陈代谢(v3.0 新增,批次 2-③)
- 熄灭(久未用变暗)/ 点亮(被调用)/ 内化(高频换视觉)/ 锁定(钉住永不熄灭)
- **只看客观使用数据,不靠 AI 猜**——所以阶段一就能落地
- 不能纯按频率(会误杀重要低频),必须有锁定口子

### 蒸馏(v3.0 新增,批次 3-①)
- 阶段一只做**半自动**:观察→建议→一键确认,系统不自己写
- 全自动留阶段四,门槛=质量高到用户敢直接信

### 产品形态
- 主形态 Tauri 桌面应用;Claude Code hooks 阶段一,Codex 阶段二,Cursor 插件阶段三,浏览器扩展阶段四
- 节点 ↔ 文件双向同步:任何方向操作互相覆盖,删除走 trash,冲突走 conflicts/

---

## 四、对外说什么(好处句式 vs 承诺句式)

**铁律:说"好处"能当卖点,说"承诺"踩雷。** 动词:`帮/让/顺带`=好处(喊),`防止/避免/替代/消灭`=承诺(禁)。

**能喊的卖点(好处句式):**
- ✅ "你的偏好,所有 AI 都能读" / "Claude/Cursor/Codex 通用"
- ✅ "你的私有资产,不属于任何 AI 公司"
- ✅ "按你当前在干的活实时挑最相关的几条喂给 AI,不是死配置"
- ✅ "用得越久,它越像你"
- ✅ "点开一个节点就是打开你大脑的一块"(不喊"替代文件树")

**禁区(承诺句式,会被拆穿):**
- ❌ "看清 Agent 大脑在想什么" / "避免幻觉" / "防止漂移" / "替代文件树" / "成为唯一大脑" / "贾维斯/数字分身"

详见 memory starnet-marketing-redlines。

---

## 五、技术难点(要解决到位的硬骨头)

痛点客观(跨工具重写偏好确实疼),不验证有没有人疼。要啃的硬骨头:

1. **活注入精度**——三层打分要真能按当前任务选对偏好,不塞错不漏关键的。这是护城河①的命。
2. **节点新陈代谢**——熄灭/内化要准,不误杀重要低频节点(靠锁定兜底)。这是护城河②甩开 Obsidian 的命。
3. **跨 Agent 生效程度不一**——Claude 严格、Cursor 静态文件只能生成/覆盖、Codex 又不同,精度要可量化(阶段二)。
4. **数据零损耗迁移**——markdown 本地、星网消失也能用,主权根基不打折。
5. **节点图空间感**——做出"打开自己大脑"的感觉,极简(奥卡姆+六度分离),震撼来自留白不是堆视觉。
6. **半自动蒸馏建议质量**——建议要到"你愿意点确认"的程度,不制造噪音(批次3);全自动蒸馏质量(阶段四)。

---

## 六、竞品/参考(2026-05 web 检索)

**行业标准(直接冲击原卖点,已认降级):**
- **AGENTS.md** —— OpenAI 发起、Linux 基金会 Agentic AI Foundation 治理,Cursor/Codex/Claude Code/Copilot/Windsurf/Antigravity 全原生支持。"一份规则各工具读"被它白送 → 星网把同步降级为地基,抄它。

**同类工具(几十个,赛道挤,几乎全是 project 级+静态文件+开发者 CLI):**
- rulz.io、sync.aligntrue.ai、ruler、amgr、amtiYo/agents(67★,同步 instructions/MCP/skills)、roderik/ai-rules。
- **AgentMemory**(6.8k★):跨工具本地记忆,做了主柱大部分,但开发者风格,目标用户跟 vibecoder 不重合。

**没人做的(=星网活路):** personal 级(跟人走)+ 活注入(非死文件)+ 给 vibecoder 的桌面/节点图体验 + 新陈代谢(会减的图)。这个组合没开源、没商品化。

**拿来用:** 底层管道(同步/适配/MCP/hooks)直接抄开源,力气全砸"活注入+vibecoder 体验"两条护城河。

---

## 七、协作模式

- Claude:工程师 + 架构师,出代码、出方案、攻技术
- 用户:产品负责人 + 老板,做决策、用产品、和真实用户沟通

---

## 八、下次接续怎么开始

1. **先读** 本文件第一节(⭐最新对话结论 + 当前到哪了)
2. **要干活** 读 `星网_执行计划.md`(怎么做、哪个批次)→ 当前下一步是批次 1-① 偏好编辑器
3. **要对齐方向** 先读 `星网_产品全局plan.md`(北极星·一页看清最终形态,2026-06-08 干净版);要愿景细节/竞品/营销红线再翻归档的 `05_DECISIONS/星网_产品plan_v3详版_归档.md`
4. **要写代码** 读 `参考文档/星网_数据格式宪法.md` + `参考文档/星网_产品决策清单.md`
