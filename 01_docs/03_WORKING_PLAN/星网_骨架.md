# 星网 StarNet · 骨架(唯一真相源)

> 🟢 **基线:2026-06-09 创始人拍板。这份骨架 = 唯一真相源。**
> **以后所有内容都在这个骨架上增量讨论;之前的文档(`星网_定位纠正_进行中.md` / `星网_产品全局plan.md` / `星网_执行思维导图` 等)一律视为过时,需按本骨架返工,不得反过来影响骨架。**
> 骨架只放结构(痛点 / 核心 / 目的 / 护栏)。功能、实现、视觉、批次 = "肉",见末尾「待填肉清单」,到填肉那步才往里放。
> 2026-06-11 增补:① 痛点分硬/软,软痛(盲区)不承重;② 镜子改为"为治理服务",锚在硬痛上;③ 机制行加"生死线"(AI 读的必须是星网治理的那份)。

---

```
星网

├ 痛点
│   · AI 记忆熵增成噪音(硬痛,承重)
│       - 越用越多/乱/膨胀:旧的不暗、错的还在、重复叠加
│       - AI 抓一把喂自己,抓到过时/矛盾的 → 帮倒忙
│       - 单工具就发生
│   · 人对自己有盲区(软痛,只当愿景,不承重)
│       - 没一个东西完整装着"你怎么做事"
│       - 看不清自己的套路,更看不见思维里的问题
│
├ 核心 = 记忆治理层
│   机制:把各家 memory 从 agent 里拆出来、归一化成一份归你的  ← 三柱的前提
│   ⚠️ 生死线:AI 读的那份必须就是星网治理的那份(Claude Code 已验证通)
│       AI 读不到的治理 = 白干;每个新功能先过这一问
│   · 看见(镜子)——为治理服务:看不见就清不了,先看见才能减负
│       - 照关系/状态/模式:亮度=哪条热、连线=谁推翻谁、聚类=你的套路
│       - "照出你的套路/盲区"是赠品和愿景,不承重、不当卖点
│       - 内容退到点开才显,不是把清单画成点再列一遍
│   · 减负(代谢)
│       - 没用的变暗靠边、要用随时点亮;真删只有被明确推翻的(可恢复)
│       - 留遗忘去噪的好处,扔掉真丢的坏处
│   · 归你(本地)
│       - markdown 本地、可迁移、星网没了也能用
│       - 你随时能走
│
├ 目的 = 让你成为更强的自己
│   两条腿:
│     · 认识自己:星网镜子直接照出你的套路/盲区(自给自足,不经 AI)
│     · 放大产出:AI 拿干净的"你",把你做事的能力放大(经 AI)
│   资产化:你的做事方式/工作流被提炼、沉淀成归你的可复用资产(越积越值钱)
│   互喂(飞轮):
│     越认识自己 → 理得越准 → AI 放大得越贴
│     借 AI 做成的事越多 → 镜子照见的"你"越丰富 → 越认识自己  ↺
│   终极=贾维斯:了解你、放大你、关键时刻提醒,但从不替你决定
│
└ 护栏
    · 只呈现不接管(提醒停觉察级,你拍)
    · 跨工具捎带(单工具就值,多工具是放大不是前提)
    · 读取永远是 agent 自己干的(hook 也是官方门,agent 自己来取):星网只把文件备好(贴合+同步+粗选片)
      星网绝不卡在读取路上硬塞/当中间商;星网死了 agent 照常活
    · 自给自足(不接 agent 也是能用的镜子+治理台)
    · 变暗不删 · 不卷模型 · 话术不承诺
```

---

## 待填肉清单(到填肉那步统一放,别污染骨架)

1. 文件聚合呈现 + 点击即编辑 + 不搬走、实时同步(工程文件 + 记忆文件,星网集中呈现,改星网即改原文件)。
2. 代谢的具体行为(变暗/点亮/内化/锁定/到点清临时)。
3. 节点图怎么照关系/状态/模式的具体画法。
4. 贴合同步的两种形状(写静态文件 / 注册 MCP 源)+ 粗选片。
5. 主动性/睡眠期(后台自己回放→蒸馏→代谢、主动照模式,默认你亲手开)= 减负的最高形态。

## 暂挂(创始人 2026-06-09 定:先不管)
- 受众分层(愿景=所有用 agent 的人 / 切入=vibecoder):本质是"给谁",不进这 4 根骨,以后放 plan 单列。
