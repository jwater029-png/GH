# GH 执行计划

## 这个目录是干什么的

- 这里放 `GH` 当前正在执行的计划。
- 这里只跟踪当前激活项和下一步，不承担长期规则或灵感收口。

## 总目标

在一个干净、隔离的工作区里定义并推进 `GH` 这个新产品。

## 当前模块

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 产品定义 | 进行中 | 先把 `GH` 是什么说清，再扩大实现 |
| 执行准备 | 进行中 | 保持工作区干净且隔离 |
| 构建实现 | 待开始 | 等产品方向确认后再进入 |

## 当前焦点

- 当前模块：`执行准备`
- 当前子模块：`工作区隔离与协作骨架`
- 当前动作：`用 docs 文件夹结构作为当前项目骨架`
- 下一步：`等产品方向更明确后，再补项目专属内容`

## 约束

- 不自动继承 Stillfield 的产品逻辑
- 不自动继承旧项目记忆
- 不对 `C:\Users\ghqah\ClaudeProjects` 形成隐藏依赖

## 当前状态

- `GH` 已存在于 `D:\Projects\GH`
- docs 文件夹结构已启用
- 隔离规则已生效

## 后续怎么长

- 项目还小时，当前激活计划先直接放在这个 `README.md`。
- 只有当某一部分内容多到难读时，再拆成子文件。
- 产品方向还没确认前，不要把不确定想法伪装成正式计划结构。
