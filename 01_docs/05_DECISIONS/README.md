# GH 已确认决策

## 这个目录是干什么的

- 这里存已经确认生效的决策。
- 只有方向真正定下来后，才写进这里。

## 2026-05-25

- `GH` 物理隔离为独立项目
- 全局母系统独立存在
- `GH` 在 `01_docs` 中只保留项目专属文件
- 全局系统是未来工作的唯一默认全局规则来源
- `GH` 默认不继承 Stillfield 的产品逻辑
- 全局协作习惯可以沿用
- `01_docs` 改为规则、计划、想法、决策四个可展开目录

## 后续怎么长

- 决策还不多时，先直接记在这个 `README.md`。
- 决策多到不好扫时，再按月份或主题拆开。
- 没定的开放问题不要放这里。
