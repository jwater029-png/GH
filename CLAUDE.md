# GH 项目入口

此文件是 `GH` 项目的项目级入口。

读取顺序：

1. 先读取 `C:\Users\ghqah\projects\_SYSTEM\AGENTS.md`
2. 再读取当前文件

项目说明：

- `GH` 是当前项目根目录。
- 项目级规则、约束、长期说明只在本项目内维护。
- 如需新增项目规则，统一追加在当前文件中，不回写到 `C:` 下的入口文件。

---

## 星网产品硬规则

- **唯一核心 plan = `01_docs/03_WORKING_PLAN/星网_骨架.md`**（2026-06-11 起生效）。所有产品讨论以它为准；`_过时归档_2026-06-11/` 里的旧文档只可考古不可引用。
- 产品 = AI 记忆治理层（看见镜子 / 减负代谢 / 归你本地）。跨工具是捎带手，不是前提。
- 生死线：AI 读的那份必须就是星网治理的那份；每个新功能先过这一问。
- 任何新想法先问"从骨架哪根骨长出来"，长不出的不立项；再过骨架五条护栏，撞线的砍。
- 门槛用能力指标，不用用户人数；只问"痛点解决够不够硬"，不问"用户喜不喜欢"
- 文案动词锁死在"帮/让/顺带"，禁用"防止/避免/替代/消灭"
- 营销禁区见 memory `starnet-marketing-redlines`
