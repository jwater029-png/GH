# GH 项目入口

此文件是 `GH` 项目的项目级入口。

读取顺序：

1. 先读取 `C:\Users\ghqah\projects\_SYSTEM\AGENTS.md`
2. 再读取当前文件

项目说明：

- `GH` 是当前项目根目录。
- 项目级规则、约束、长期说明只在本项目内维护。
- 如需新增项目规则，统一追加在当前文件中，不回写到 `C:` 下的入口文件。

---

## 星网产品硬规则

- 产品是给 vibecoder 造的本地大脑（三层：地基=跨工具同步 / 主柱=偏好存储 / 灵魂=节点图新陈代谢）
- 任何新想法先问"从哪层长出来"，长不出的不立项
- 门槛用能力指标，不用用户人数；只问"痛点解决够不够硬"，不问"用户喜不喜欢"
- 文案动词锁死在"帮/让/顺带"，禁用"防止/避免/替代/消灭"
- 详细产品定义见 `01_docs/`，营销禁区见 memory `starnet-marketing-redlines`
